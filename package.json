{
  "name": "database_concurrency",
  "version": "1.0.0",
  "description": "A simulation of a race condition to demonstrate how multiple users attempting to access the same resource at the same time can result in unexpected errors.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js"
  },
  "author": "Dev Cisco",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.1",
    "mysql2": "^3.9.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
